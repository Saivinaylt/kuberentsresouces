# kind: Pod
# apiVersion: v1
# metadata:
#   name: firstpod
#   namespace: roboshop
#   labels:
#     name: web
#     kind: application
# spec:
#   containers:
#   - image: nginx
#     name: weapp
#     conrainerPort: 80
apiVersion: v1
metadata:
  name: labelpod
  namespace: roboshop
  labels:
    name: saivinay
    dept: cse
    year: twenty
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
    - configMapRef:
        name: configmap
